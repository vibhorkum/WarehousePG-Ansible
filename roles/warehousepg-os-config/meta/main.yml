---
galaxy_info:
  author: WarehousePG Team
  description: Ansible role for WarehousePG OS configuration and tuning
  company:
  license: MIT
  min_ansible_version: 2.9
  platforms:
    - name: EL
      versions:
        - 8
        - 9
  galaxy_tags:
    - database
    - postgresql
    - greenplum
    - warehousepg
    - system
    - tuning

dependencies: []

# Required collections (install with: ansible-galaxy collection install -r requirements.yml)
# - community.general (for sysctl, firewalld modules)
# - ansible.posix (for selinux module)
